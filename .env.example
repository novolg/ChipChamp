# Poker Tutor is fully client-side — no backend, no API keys, no secrets at this time.
# This file exists per project convention. Add VITE_-prefixed vars here if/when needed.
# Example:
# VITE_SOME_FLAG=false
